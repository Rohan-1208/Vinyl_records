# Frontend (Vite)
# Set to your backend origin. No trailing slash. Do NOT include "/api".
# Examples: https://api.yourdomain.com  |  https://vinyl-api.onrender.com
VITE_BACKEND_URL=
    
# Backend (FastAPI) – set these on your backend host (e.g., Render)
# URL of your deployed frontend (Vercel/custom domain). No trailing slash.
FRONTEND_URL=

# Spotify credentials from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Must match the redirect URI configured in the Spotify dashboard
# Format: <backend-origin>/auth/spotify/callback
# Example: https://vinyl-api.onrender.com/auth/spotify/callback
SPOTIFY_REDIRECT_URI=